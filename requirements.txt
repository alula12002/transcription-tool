# Core
openai>=1.0.0
anthropic>=0.18.0
pydub>=0.25.1
pydantic>=2.0.0
python-dotenv>=1.0.0

# Python 3.13 compatibility
# pydub depends on audioop, which was removed from stdlib in Python 3.13.
# audioop-lts is a drop-in replacement that only installs on 3.13+.
# On 3.12 and earlier, this line is ignored (audioop is still built-in).
audioop-lts>=0.2.1; python_version >= "3.13"

# FastAPI backend
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
